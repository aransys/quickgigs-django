#!/usr/bin/env bash
#
# DEPRECATED — this script was a one-shot used during the v2 overhaul to
# remove pre-overhaul files from the repo (committed venvs, leaked .env,
# bloated docs, etc.). It has done its job. Safe to delete.
#
# If you ever need to recover its history, see the project's git log
# around the v2.0.0 tag.

echo "scripts/cleanup-legacy.sh is deprecated and does nothing."
echo "Safe to delete: rm scripts/cleanup-legacy.sh"
